# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

CS2 (Counter-Strike 2) demo processor / analyzer. Currently a fresh repository — no source code, build system, or tests exist yet. Update this file as those pieces land.

## Commands

_To be filled in once a build system is chosen. Capture here:_
- How to install dependencies
- How to build / run
- How to run the full test suite
- How to run a single test
- Lint / format / typecheck

## Architecture

_To be filled in once the first modules exist. Focus on the big-picture parts that aren't obvious from reading a single file — e.g., how a `.dem` file flows through parsing → event extraction → analysis → output, and where the seams between those stages live._
